# Docker image configuration
docker.image.name=jbake/texy-service
# docker.image.tag is set from project.version

# Skip Docker operations (useful in environments without Docker)
# Can be overridden with -PskipDocker=true
#skipDocker=false

# Docker Hub credentials (can also be set via environment variables)
# DOCKERHUB_USERNAME and DOCKERHUB_PASSWORD
#dockerhub.username=
#dockerhub.password=

